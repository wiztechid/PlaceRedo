const ids=["length","width","height","doors","windows","coats","tier"];function calc(){const v=Object.fromEntries(ids.map(id=>[id,+document.getElementById(id).value]));const gross=2*(v.length+v.width)*v.height;const net=Math.max(0,gross-v.doors*21-v.windows*15);const gallons=Math.max(1,Math.ceil(net*v.coats/350));document.getElementById("area").textContent=Math.round(net).toLocaleString()+" sq ft";document.getElementById("gallons").textContent=gallons+" gal";document.getElementById("budget").textContent="$"+(gallons*v.tier).toLocaleString();}ids.forEach(id=>document.getElementById(id).addEventListener("input",calc));calc();